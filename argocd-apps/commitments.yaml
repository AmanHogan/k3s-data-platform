# ArgoCD Application: watches manifests/commitments in this infra repo and keeps
# the cluster in sync with it. Apply this once; after that, any change Jenkins
# pushes to that path auto-deploys.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: commitments
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/AmanHogan/k3s-data-platform.git
    targetRevision: master
    path: manifests/commitments
  destination:
    server: https://kubernetes.default.svc
    namespace: commitments
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

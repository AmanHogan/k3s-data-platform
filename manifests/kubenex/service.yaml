apiVersion: v1
kind: Service
metadata:
  name: kubenex
  namespace: kubenex
  labels:
    app: kubenex
spec:
  type: NodePort
  selector:
    app: kubenex
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30900

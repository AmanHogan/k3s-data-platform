apiVersion: v1
kind: Service
metadata:
  name: commitments
  namespace: portfolio
  labels:
    app: commitments
spec:
  # ClusterIP — Cloudflare Tunnel reaches this via in-cluster DNS:
  #   commitments.portfolio.svc.cluster.local:3000
  # No public LoadBalancer or NodePort needed.
  type: ClusterIP
  selector:
    app: commitments
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP

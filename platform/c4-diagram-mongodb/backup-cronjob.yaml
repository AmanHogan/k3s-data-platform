# Daily MongoDB backups via `mongodump`, mirroring the commitments pattern.
#
#  - Writes a single gzipped archive per run to a dedicated `mongodb-backups` PVC.
#  - Keeps the most recent 7 archives, prunes older ones.
#  - The Job pod is pinned to the k3s-agent node (nodeSelector) — the live
#    mongo pod for this app landed on k3s-server (verified after first
#    deploy), so the backup runs on the other node from the live data.
#
# Restore (manual): copy an archive out and run, e.g.:
#   kubectl exec -it deploy/mongodb -n c4-diagram -- sh -c \
#     'mongorestore --gzip --archive=/path/to/archive --drop'
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-backups
  namespace: c4-diagram
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: mongodb-backup
  namespace: c4-diagram
spec:
  schedule: "30 3 * * *" # 03:30 daily — offset from commitments' 03:00 run
  timeZone: "America/Chicago"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 86400
      template:
        spec:
          restartPolicy: OnFailure
          nodeSelector:
            kubernetes.io/hostname: k3s-agent
          containers:
            - name: mongodump
              image: mongo:4.4
              command:
                - /bin/sh
                - -c
                - |
                  set -e
                  TS=$(date +%Y%m%d-%H%M%S)
                  echo "Starting backup c4-diagram-${TS}..."
                  mongodump \
                    --host=mongodb.c4-diagram.svc.cluster.local \
                    --port=27017 \
                    --gzip \
                    --archive=/backups/c4-diagram-${TS}.archive.gz
                  echo "Backup written. Pruning to the latest 7..."
                  ls -1t /backups/c4-diagram-*.archive.gz | tail -n +8 | xargs -r rm -f
                  echo "Current backups:"
                  ls -lh /backups/
              volumeMounts:
                - name: backups
                  mountPath: /backups
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 256Mi
          volumes:
            - name: backups
              persistentVolumeClaim:
                claimName: mongodb-backups
